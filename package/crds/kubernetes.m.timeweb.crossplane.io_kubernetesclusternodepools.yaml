---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: kubernetesclusternodepools.kubernetes.m.timeweb.crossplane.io
spec:
  group: kubernetes.m.timeweb.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - timeweb
    kind: KubernetesClusterNodepool
    listKind: KubernetesClusterNodepoolList
    plural: kubernetesclusternodepools
    singular: kubernetesclusternodepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.atProvider.clusterID
      name: CLUSTER
      type: string
    - jsonPath: .spec.forProvider.presetName
      name: PRESET
      type: string
    - jsonPath: .spec.forProvider.nodeCount
      name: DESIRED
      type: integer
    - jsonPath: .status.atProvider.observedNodeCount
      name: OBSERVED
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubernetesClusterNodepool is one Timeweb managed-Kubernetes worker
          group.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesClusterNodepoolSpec is the desired state.
            properties:
              forProvider:
                description: |-
                  KubernetesClusterNodepoolParameters is the operator-settable surface for
                  one upstream worker group. See spec.md FR-009 and
                  contracts/kubernetesclusternodepool-v1alpha1.md.
                properties:
                  autohealing:
                    description: |-
                      Autohealing enables automatic recovery of failed nodes. Immutable
                      post-create.
                    type: boolean
                  autoscaling:
                    description: |-
                      Autoscaling enables the upstream cluster-autoscaler for this group.
                      Immutable post-create.
                    properties:
                      enabled:
                        type: boolean
                      maxSize:
                        minimum: 2
                        type: integer
                      minSize:
                        minimum: 2
                        type: integer
                    required:
                    - enabled
                    - maxSize
                    - minSize
                    type: object
                  clusterID:
                    type: string
                  clusterRef:
                    description: |-
                      ClusterRef / ClusterSelector / ClusterID reference the parent
                      KubernetesCluster. Exactly one MUST be set. Immutable post-create.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterSelector:
                    description: A Selector selects an object.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels are Kubernetes node labels applied to the group. Marshaled to
                      the upstream array<{key,value}> shape on create. Immutable post-create.
                    type: object
                  name:
                    description: Name of the worker group. Immutable post-create.
                    minLength: 1
                    type: string
                  nodeCount:
                    description: |-
                      NodeCount is the desired worker count. Mutable when autoscaling is
                      off (scaled via relative add/remove deltas). Ignored when autoscaling
                      is on.
                    maximum: 100
                    minimum: 1
                    type: integer
                  presetName:
                    description: |-
                      PresetName is the worker-node preset slug resolved against
                      /api/v1/presets/k8s (type=worker) to the upstream preset_id.
                      Immutable post-create. Exactly one of presetName/resources (CEL).
                    pattern: ^[a-z0-9][a-z0-9-]*[a-z0-9]$
                    type: string
                  resources:
                    description: |-
                      Resources is the custom-configurator sizing path for the workers
                      (cpu/ramGB/diskGB + optional gpu) — alternative to presetName.
                      Immutable post-create.
                    properties:
                      cpu:
                        minimum: 1
                        type: integer
                      diskGB:
                        minimum: 1
                        type: integer
                      gpu:
                        type: integer
                      ramGB:
                        minimum: 1
                        type: integer
                    required:
                    - cpu
                    - diskGB
                    - ramGB
                    type: object
                required:
                - name
                - nodeCount
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: KubernetesClusterNodepoolStatus is the observed state.
            properties:
              atProvider:
                description: KubernetesClusterNodepoolObservation is the observed
                  state.
                properties:
                  clusterID:
                    description: |-
                      ClusterID is the resolved parent cluster id, persisted so Observe and
                      Delete (which need the cluster_id in the path) never depend on a live
                      ref lookup.
                    type: string
                  lockedConfiguratorID:
                    description: |-
                      LockedConfiguratorID is the upstream configurator id resolved at Create
                      when sized via `resources` (drives sizing-variant-switch detection).
                    format: int64
                    type: integer
                  lockedPresetID:
                    description: LockedPresetID is the upstream worker preset_id resolved
                      at Create.
                    format: int64
                    type: integer
                  nodes:
                    description: |-
                      Nodes lists the group's actual worker nodes (mirrors the dashboard's
                      per-group node table). Readiness is derived from these statuses — the
                      group-level node count alone is echoed before any VM exists.
                    items:
                      description: NodepoolNode is one worker node of the group as
                        reported upstream.
                      properties:
                        createdAt:
                          description: CreatedAt is the upstream node creation timestamp.
                          type: string
                        id:
                          description: ID is the upstream node id.
                          format: int64
                          type: integer
                        ip:
                          description: IP is the node's local network IP.
                          type: string
                        status:
                          description: Status is the raw upstream node state (e.g.
                            installing, active).
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  observedNodeCount:
                    description: ObservedNodeCount is the upstream node count.
                    type: integer
                  upstreamID:
                    description: UpstreamID is the Timeweb worker-group id (external-name).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: exactly one of clusterRef, clusterSelector, clusterID must be set
          rule: (has(self.spec.forProvider.clusterRef)?1:0) + (has(self.spec.forProvider.clusterSelector)?1:0)
            + (has(self.spec.forProvider.clusterID)?1:0) == 1
        - message: exactly one of presetName or resources must be set
          rule: (has(self.spec.forProvider.presetName)?1:0) + (has(self.spec.forProvider.resources)?1:0)
            == 1
        - message: autoscaling.maxSize must be >= autoscaling.minSize
          rule: '!has(self.spec.forProvider.autoscaling) || !self.spec.forProvider.autoscaling.enabled
            || self.spec.forProvider.autoscaling.maxSize >= self.spec.forProvider.autoscaling.minSize'
    served: true
    storage: true
    subresources:
      status: {}
